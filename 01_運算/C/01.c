#include <stdio.h>

int main(){
    int a, b;
    scanf("%d %d", &a, &b);
    printf("The first variable has the decimal value: %d\n", a);
    printf("The second variable has the decimal value: %d\n", b);

    return 0;
}