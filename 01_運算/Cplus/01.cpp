#include <iostream>

using namespace std;

int main(){
    int a, b;
    cin >> a >> b;
    cout << "The first variable has the decimal value: " << a << endl;
    cout << "The second variable has the decimal value: " << b << endl;

    return 0;
}